import { NgModule } from '@angular/core';
import { HomeCardComponent } from './home-card.component';

@NgModule({
  declarations: [
    HomeCardComponent
  ],
  imports: [
  ],
  providers: [HomeCardComponent],
  bootstrap: []
})
export class HomeCardModule { }
